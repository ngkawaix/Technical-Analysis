# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt  —  Technical Analysis (ML) App
# ─────────────────────────────────────────────────────────────────────────────
# Target platform : Streamlit Community Cloud (Python 3.11)
# Last validated  : June 2026
#
# Pin strategy: minor-version pinned (==X.Y.*) rather than exact patch pins.
# This lets Streamlit Cloud's resolver pull the latest compatible patch while
# keeping the API surface stable. Exact pins are used only where breaking
# changes are known at the minor boundary (TensorFlow, XGBoost).
#
# NOTE: tensorflow is the largest dependency (~600 MB). If cold-start time
# becomes a problem, swap to tensorflow-cpu which is ~200 MB lighter and
# sufficient since Streamlit Cloud has no GPU.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web framework ────────────────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0

# ── Data & numerics ───────────────────────────────────────────────────────────
numpy>=1.26.0,<3.0.0
pandas>=2.2.0,<4.0.0
scipy>=1.12.0,<2.0.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.40,<1.0.0
requests>=2.31.0            # yfinance transitive dep — explicit for reliability

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly>=5.20.0,<6.0.0

# ── Machine learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4.0,<2.0.0
xgboost>=2.0.0,<4.0.0
hmmlearn>=0.3.0,<1.0.0

# ── Deep learning (LSTM) ─────────────────────────────────────────────────────
# tensorflow-cpu is recommended over tensorflow on Streamlit Cloud:
#   - ~200 MB smaller install (no CUDA libs)
#   - identical Python API — only change is no GPU acceleration (not needed here)
# To switch, replace the line below with: tensorflow-cpu>=2.16.0,<3.0.0
tensorflow>=2.16.0,<3.0.0
